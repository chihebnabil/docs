---
title: "next-lovable CLI"
description: "Automatically migrate React applications to Next.js 13+ App Router with intelligent React Router conversion"
icon: "rocket"
---

<Card title="What is next-lovable?" icon="lightbulb">
  A smart CLI tool that automatically converts your React applications to Next.js 13\+ using the new App Router. It specializes in migrating React Router applications with zero manual work.
</Card>

## ✨ Key Features

<CardGroup cols={2}>
  <Card title="Smart Router Conversion" icon="route">
    Automatically converts React Router to Next.js App Router
  </Card>
  <Card title="Free Single File Testing" icon="file-code">
    Test individual components without credits or authentication
  </Card>
  <Card title="Vite to Next.js Migration" icon="box">
    Seamlessly migrates from Vite build system to Next.js
  </Card>
  <Card title="Intelligent Components" icon="brain">
    Automatically adds 'use client' directives where needed
  </Card>
</CardGroup>

## 🚀 Get Started in 2 Minutes

<Steps>
  <Step title="Install globally">
    ```bash
    npm install -g next-lovable
    ```
  </Step>
  <Step title="Try your first conversion (free!)">
    ```bash
    next-lovable convert MyComponent.tsx --dry-run --show-diff
    ```
  </Step>
  <Step title="Apply the conversion">
    ```bash
    next-lovable convert MyComponent.tsx
    ```
  </Step>
</Steps>

<Info>
  **No account needed** for single file conversions! Perfect for testing and learning.
</Info>

## Documentation

<CardGroup cols={3}>
  <Card title="Installation Guide" href="/0.0.7/installation" icon="download">
    System requirements and setup instructions
  </Card>
  <Card title="Quick Start" href="/0.0.7/quick-start" icon="play">
    Your first conversion in 5 minutes
  </Card>
  <Card title="Convert Command" href="/0.0.7/commands/convert" icon="file-arrow-right">
    Free file conversion (no auth required)
  </Card>
  <Card title="Migrate Command" href="/0.0.7/commands/migrate" icon="folder-arrow-right">
    Full project migration (requires account)
  </Card>
  <Card title="Troubleshooting" href="/0.0.7/guides/troubleshooting" icon="wrench">
    Common issues and solutions
  </Card>
  <Card title="Best Practices" href="/0.0.7/guides/best-practices" icon="star">
    Tips for successful migrations
  </Card>
</CardGroup>

## What You Can Do

### Free Features (No Auth Required)

<CardGroup cols={2}>
  <Card title="Single File Conversion" icon="file">
    Convert individual React files to Next.js format
  </Card>
  <Card title="Preview Changes" icon="eye">
    See exactly what will change before applying
  </Card>
</CardGroup>

### Premium Features (Account Required)

<CardGroup cols={2}>
  <Card title="Full Project Migration" icon="folder">
    Convert entire React applications to Next.js
  </Card>
  <Card title="Batch Processing" icon="layers">
    Convert multiple files and directories at once
  </Card>
</CardGroup>

## What Gets Converted

<AccordionGroup>
  <Accordion title="React Router → Next.js Navigation">
    - `useNavigate()` → `useRouter()` from `next/navigation`
    - `<Link to="/path">` → `<Link href="/path">` from `next/link`
    - `useLocation()` → `usePathname()` \+ `useSearchParams()`
    - `useParams()` → Next.js `useParams()`
  </Accordion>
  
  <Accordion title="React Helmet → Next.js Head">
    - `<Helmet>` → `<Head>` from `next/head`
    - Meta tags and title preservation
    - SEO attributes maintained
  </Accordion>
  
  <Accordion title="Client Component Detection">
    - Automatic "use client" directive addition
    - Detects React hooks, event handlers, browser APIs
    - Maintains server/client component boundaries
  </Accordion>
  
  <Accordion title="Context Provider Migration">
    - Converts React Context providers
    - Maintains context functionality in Next.js structure
    - Preserves type safety
  </Accordion>
</AccordionGroup>

## � Quick Command Reference

| Task | Command | Credits | Auth |
|------|---------|---------|------|
| Test conversion | `next-lovable convert file.tsx --dry-run` | ❌ Free | ❌ No |
| Convert file | `next-lovable convert file.tsx` | ❌ Free | ❌ No |
| Preview migration | `next-lovable migrate ./app --dry-run` | ✅ Yes | ✅ Yes |
| Full migration | `next-lovable migrate ./app ./next-app` | ✅ Yes | ✅ Yes |

## Real-World Example

<CodeGroup>

```tsx Before (React Router)
import { Link, useNavigate, useLocation } from 'react-router-dom';

export const Navigation = () => {
  const navigate = useNavigate();
  const location = useLocation();

  return (
    <nav>
      <Link to="/dashboard">Dashboard</Link>
      <button onClick={() => navigate('/settings')}>Settings</button>
      <p>Current: {location.pathname}</p>
    </nav>
  );
};
```

```tsx After (Next.js)
'use client';

import Link from 'next/link';
import { useRouter, usePathname } from 'next/navigation';

export const Navigation = () => {
  const router = useRouter();
  const pathname = usePathname();

  return (
    <nav>
      <Link href="/dashboard">Dashboard</Link>
      <button onClick={() => router.push('/settings')}>Settings</button>
      <p>Current: {pathname}</p>
    </nav>
  );
};
```

</CodeGroup>

## Important Notes

<Warning>
  **Version 0.7\+**: Uses new subcommand structure (`convert`, `migrate`)
  
  **Version 0.6 and earlier**: Uses legacy command format
</Warning>

<Tip>
  Start with the free `convert` command to get familiar with the tool before migrating entire projects.
</Tip>

## Need Help?

<CardGroup>
  <Card title="Done for you" href="https://nextlovable.com/done-for-you" icon="github">
    We'll convert your beautiful Lovable app into a lightning-fast, SEO-ready website that actually shows up in search results. No coding required.
  </Card>
</CardGroup>
---

Ready to migrate your React app to Next.js? [Start with a quick conversion →](/0.0.7/quick-start)